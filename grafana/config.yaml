name: "Grafana"
version: "0.2.2"
slug: "grafana"
description: "Grafana analytics and monitoring platform"
url: "https://github.com/bborchers/ha-addons-grafana/tree/main/grafana"
image: "ghcr.io/bborchers/ha-addons-grafana/{arch}"
arch:
  - aarch64
  - amd64
init: false
ports:
  3000/tcp: 3000
ports_description:
  3000/tcp: "Grafana Web UI"
options:
  log_level: info
  admin_user: "admin"
  admin_password: "admin"
  anonymous_access: false
schema:
  log_level: list(debug|info|warn|error|critical)
  admin_user: str
  admin_password: password
  anonymous_access: bool
